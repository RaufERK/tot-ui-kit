# @tot/ui-kit

Общий UI‑кит с `Layout`, `ScMainMenu`, `MainMenuFull` и вспомогательными компонентами (`UpperMenu`, `PageLabel` и т.д.).

> **Важно**: Библиотека использует обычный CSS (без CSS Modules). SCSS не используется — tsup собирает всё без дополнительных плагинов. В проектах на webpack убедитесь, что подключены `css-loader` и `style-loader` (или `mini-css-extract-plugin` для продакшена). В Vite всё работает из коробки.

## Установка в проект

### Вариант 1 — через Git URL (текущий рабочий)

```bash
npm install git+ssh://git@sberworks.ru:7998/~erk.r/tot-ui-kit.git
```

Или в `package.json`:

```jsonc
"dependencies": {
  "@tot/ui-kit": "git+ssh://git@sberworks.ru:7998/~erk.r/tot-ui-kit.git",
  "react": "^18.3.1",
  "react-dom": "^18.3.1"
}
```

### Подключение глобальных стилей

Входной файл приложения (например, `src/main.tsx`):

```ts
import '@tot/ui-kit/global.css';
import '@sberbusiness/triplex-next/styles/triplex-next.css'; // если используете Triplex компоненты
import '@sberbusiness/icons-next/styles/icons.css';         // если нужны иконки
```

> **Тёмная тема**: `global.css` автоматически переопределяет CSS‑переменные Triplex (`--triplex-next-Typography-*`, `--triplex-next-Card-*` и др.) при `data-theme="dark"` на `<html>`. Переключение темы встроено в `Layout`.

## Быстрый старт

```tsx
import { Layout } from '@tot/ui-kit';

const API_BASE = import.meta.env.VITE_API_BASE ?? 'http://localhost:4000';

export const App = () => (
  <Layout
    menuProps={{
      baseUrl: API_BASE,
      menuId: 'VKIIw4zpK-wnEuFag4GXO',
      activeAppId: 'sc',
      systemTitle: 'Центр установок',
    }}
  >
    {/* контент страниц */}
  </Layout>
);
```

### Кастомизация цвета фона меню

С версии **0.0.2** можно настроить цвет фона бокового меню для тёмной темы через проп `menuBackgroundColor`:

```tsx
<Layout
  menuProps={{
    baseUrl: API_BASE,
    menuId: 'VKIIw4zpK-wnEuFag4GXO',
    activeAppId: 'sc',
    systemTitle: 'Центр установок',
  }}
  menuBackgroundColor="#1e293b"  // кастомный цвет для тёмной темы
>
  {/* контент страниц */}
</Layout>
```

**По умолчанию** используется цвет `#363a3f` (тёмно-серый из корпоративного дизайна).

**Примеры использования:**

```tsx
// Дефолтный цвет (если не передавать проп)
<Layout menuProps={{...}} />

// Slate-оттенок
<Layout menuProps={{...}} menuBackgroundColor="#1e293b" />

// Тёмно-серый
<Layout menuProps={{...}} menuBackgroundColor="#1f2937" />

// Чисто чёрный
<Layout menuProps={{...}} menuBackgroundColor="#000000" />

// Прозрачный (наследует фон от родительского элемента)
<Layout menuProps={{...}} menuBackgroundColor="transparent" />
```

> 💡 **Примечание**: Цвет применяется только в тёмной теме. В светлой теме меню использует цвет из дизайн-системы Triplex.

## Основные пропсы

- `Layout`
  - `menuProps` — любые пропсы `ScMainMenu` кроме `theme`/`layout`. Достаточно передать `baseUrl` или `apps`.
  - `initialMenuLayout`, `initialTheme` — стартовое состояние меню.
  - `menuBackgroundColor` — кастомный цвет фона бокового меню в тёмной теме (по умолчанию `#363a3f`).
  - `upperMenuSlot` — кастомизация верхнего меню (если нужно).
- `ScMainMenu`
  - `baseUrl` + `menuId` (по умолчанию `VKIIw4zpK-wnEuFag4GXO`) или `dataUrl` — откуда забрать данные о приложениях.
  - `apps` — можно передать готовый список вместо загрузки.
  - `useMockData` — включить встроенный mock‑режим на основе `singleMenuData.json` (для разработки/демо без бэка).
  - `iconResolver` — функция, которая строит `AppDescriptor` из ответа бэка.
  - `onLayoutChange`, `onThemeChange` — события переключения состояния меню.

## Скрипты разработки и релиза

Из корня `tot-ui-kit`:

```bash
npm run dev         # локальная разработка библиотеки (watch-сборка через tsup)
npm run typecheck   # проверка типов (tsc --noEmit)
npm run build       # сборка библиотеки в dist/ (ESM + CJS + d.ts)
```

### Подготовка и публикация релиза

Скрипты настроены под публикацию в настроенный npm‑registry (например, корпоративный Nexus):

```bash
npm run release:patch   # patch-версия: 0.0.1 -> 0.0.2
npm run release:minor   # minor-версия: 0.0.1 -> 0.1.0
npm run release:major   # major-версия: 0.x.y -> 1.0.0
```

Внутри они выполняют:
- `npm version <level>` — обновление `version` в `package.json` и создание git‑тега (если репозиторий под git),
- `npm publish` — публикация пакета в текущий registry.

Перед любой ручной командой `npm publish` автоматически запустится:

```bash
npm run prepublishOnly  # npm run typecheck && npm run build
```

Так мы гарантируем, что в реестр уходит собранная и типобезопасная версия библиотеки.
